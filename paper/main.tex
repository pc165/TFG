\documentclass[10pt,a4paper,twocolumn,twoside]{article}
\usepackage[utf8]{inputenc}
\usepackage[catalan]{babel}
\usepackage{multicol}
\usepackage{graphicx}
\usepackage{float}
\usepackage{fancyhdr}
\usepackage{times}
\usepackage{titlesec}
\usepackage{multirow}
\usepackage{cleveref}
\usepackage{lettrine}
\usepackage{lscape}
\usepackage[top=2cm, bottom=1.5cm, left=2cm, right=2cm]{geometry}
\usepackage[figurename=Fig.,tablename=TAULA]{caption}
\captionsetup[table]{textfont=sc}

\newcommand\pref[1]{[\ref{#1}]}

\author{\LARGE\sffamily Pablo Chen}
\title{\Huge{\sffamily Disseny i implementació d'un joc de puzle tipus Sudoku per a aprendre els números en braille incorporant dispositius d'interacció gestual}}
\date{}

\newcommand\blfootnote[1]{%
    \begingroup
    \renewcommand\thefootnote{}\footnote{#1}%
    \addtocounter{footnote}{-1}%
    \endgroup
}

\titleformat{\section}
{\large\sffamily\scshape\bfseries}
{\textbf{\thesection}}{1em}{}

\begin{document}

\fancyhead[LO]{\scriptsize Pablo Chen: Disseny i implementació d'un joc de puzle tipus Sudoku per a aprendre els números en braille}
\fancyhead[RO]{\thepage}
\fancyhead[LE]{\thepage}
\fancyhead[RE]{\scriptsize EE/UAB TFG INFORMÀTICA:\@Disseny i implementació d'un Sudoku en braille}

\fancyfoot[CO,CE]{}

\fancypagestyle{primerapagina}
{
    \fancyhf{}
    \fancyhead[L]{\scriptsize TFG EN ENGINYERIA INFORMÀTICA, ESCOLA D'ENGINYERIA (EE), UNIVERSITAT AUTÒNOMA DE BARCELONA (UAB)}
    \fancyfoot[C]{\scriptsize juny de 2022, Escola d'Enginyeria (UAB)}
}

\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\pagestyle{fancy}

\twocolumn[\begin{@twocolumnfalse}

        \maketitle

        \thispagestyle{primerapagina}
        \begin{center}
            \parbox{0.915\textwidth} {\sffamily \textbf{Resum--} Resum del projecte, màxim 10 línies.\\Resum del
                projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10
                línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del
                projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10
                línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del
                projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10
                línies.\\ \textbf{Paraules clau-- } Paraules clau del treball, màxim 2 línies\\ \\ \bigskip \\
                \textbf{Abstract--} Versió en anglès del resum Resum del projecte, màxim 10 línies.\\Resum del
                projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10
                línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del
                projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10
                línies.\\Resum del projecte, màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\ projecte,
                màxim 10 línies.\\Resum del projecte, màxim 10 línies.\\ \\ \\ \textbf{Keywords-- } Versió en
                anglès de les paraules clau. \\}

            \bigskip

            {\vrule depth 0pt height 0.5pt width 4cm\hspace{7.5pt}%
                \raisebox{-3.5pt}{\fontfamily{pzd}\fontencoding{U}\fontseries{m}\fontshape{n}\fontsize{11}{12}\selectfont\char70}%
                \hspace{7.5pt}\vrule depth 0pt height 0.5pt width 4cm\relax}

        \end{center}

        \bigskip
        %\end{abstract}
    \end{@twocolumnfalse}]

\blfootnote{$\bullet$ E-mail de contacte: pablo.chen@uab.cat}
\blfootnote{$\bullet$ Menció realitzada: Enginyeria de Computadors / Computació / Enginyeria del Software / Tecnologies de la Informació}
\blfootnote{$\bullet$ Treball tutoritzat per: Enric Martí Godia (Computació)}
\blfootnote{$\bullet$ Curs 2021/22}

\section{Introducció}\label{sec:introduction}

\lettrine[lines=3] El Sudoku és un trencaclosques on el jugador tracta d'omplir una graella
parcialment emplenada de 9 $\times$ 9 caselles dividida en subgraelles de 3 $\times$ 3 (anomenades regions o blocs) amb un set de números de manera que cada fila, columna i subgraella contingui els números només una vegada.
El sudoku modern va ser popularitzat en el 2004 quan Wayne Gould va desenvolupar un programa informàtic per produir trencaclosques únics ràpidament.

Per una altra banda, el braille és un sistema d'escriptura i lectura tàctil basat en 6 punts
disposats en una graella de 3 $\times$ 3 utilitzat per a persones cegues o amb visió reduïda.

\section{Estat de l'art}\label{sec:stateOftheArt}

En l'actualitat, podem trobar sudokus en moltes parts: en els diaris, en llibres que recopilen
sudokus per resoldre, en aplicacions per al mòbil o ordinador i fins sudokus amb peces en braille.
Tot i això, no s'ha pogut trobar un sudoku 3D en braille per a ordinador que permeti aprendre els
números en braille. És per això que s'ha proposat a realitzar aquest TFG. A més a més, estic
interessat en el funcionament dels gràfics 3D.

\section{Objectiu}\label{sec:objective}
L'objectiu principal del projecte consisteix en desenvolupar un sudoku en 3D des de zero. El joc
tracta d'una graella de 9 $\times$ 9 fitxes on es visualitzarà el valor numèric amb l'alfabet
braille. Per una altra banda, mitjançant el teclat i el ratolí, el jugador podrà interactuar amb
les caselles i les fitxes per veure el valor numèric de la fitxa. Concretament, es volen aconseguir
els següents objectius:
\begin{itemize}
    \item Desenvolupar un model en 3D del sudoku.
    \item Incorporar ajudes per aprendre els números o l'alfabet en braille.
\end{itemize}

Per aconseguir-lo, es desenvoluparà un petit conjunt de classes que permetrà renderitzar objectes
en la pantalla i tractar els esdeveniments dels perifèrics.

\subsection{Requisits funcionals}\label{subsec:requisits-funcionals}

El joc haurà de gestionar els següents esdeveniments dels perifèrics i renderitzar les següents
formes geomètriques:
\begin{itemize}
    \item Pulsacions del teclat.
    \item Pulsacions del ratolí, un clic.
    \item Moviments del ratolí.
    \item Moviments de la roda del ratolí.
    \item Moviments de la finestra del joc.
    \item Canvi de la finestra del joc.
    \item Canvis d'estats de la finestra.
    \item Un cub.
    \item Una esfera.
    \item Un pla.
\end{itemize}

El joc haurà d'implementar les següents funcionalitats:
\begin{itemize}
    \item Guardar i carregar partides a partir d'un fitxer de text.
    \item Generar un sudoku aleatori d'una determinada dificultat.
    \item Donar pistes en la resolució del sudoku de manera interactiva.
    \item Aconseguir l'execució en diverses plataformes, principalment Windows i Linux.
\end{itemize}

% Resumen: presente (también pasado)
% Introducción: presente
% Materiales, metodología, procedimientos: pasado
% Resultados: pasado (o presente)
% Conclusiones: presente

\subsection{Requisits de software}\label{subsec:requisits-de-software}
Pel que fa a les llibreries, s'ha utilitzat les següents dependències:
\begin{itemize}
    \item \textit{ImGUI} Per generar la interfície d'usuari, s'ha utilitzat la versió amb viewports (branca anomenada \textit{docking}).\@
    \item \textit{GLFW} Per gestionar la inicialització de la finestra i obtenir els estats dels perifèrics.\@
    \item \textit{OpenGL} API interactuar amb la GPU.\@
    \item \textit{glbinding} Per carregar les funcions d'OpenGL.\@
    \item \textit{GLM} Una llibreria matemàtica adaptada per OpenGL.\@
\end{itemize}

Els sistemes operatius amb els quals s'ha compilat i executat el joc són Windows 10 i Fedora 35.

\subsection{Requisits de hardware}\label{subsec:requisits-de-hardware}
Per a executar el joc el sistema ha de comptar amb una GPU (pot ser integrada) que suporti OpenGL
v3.3 o major.

\subsection{Eines}\label{subsec:eines}
Respecte a les eines i llibreries de desenvolupament, s'ha utilitzat principalment, l'entorn de
desenvolupament multiplataforma \textit{CLion} per C/C++ per poder desenvolupar el joc tant en
Windows com en Linux, \textit{RenderDoc} per a diagnosticar problemes de renderització i
\textit{CMAKE} per al sistema de compilació multiplataforma.

\section{Metodologia}\label{sec:methodology}
A continuació s'expliquen la metodologia seguida i les eines utilitzades al llarg d'aquest
projecte.

\subsection{Kanban}\label{subsec:kanban}
Per a poder optimitzar al màxim els recursos necessaris, s'ha seguit una metodologia àgil,
celebrant revisions periòdiques generals en finalitzar cada sprint on es van veure els resultats de
la fase anterior, es van valorar els avenços i es va planificar el següent sprint, concretament,
s'ha utilitzat una tècnica anomenada Kanban, es van dividir les tasques en diferents subtasques i
es van col·locar cada tasca segons el progrés (to do, in progress, done). En particular, s'ha fet
servir KanbanFlow per gestionar les subtasques.

Finalment, per a gestionar el codi font, s'ha creat un repositori a GitHub on s'ha anat pujant a
ells els avenços fets.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/kanban}
    \caption{Un tauler Kanban.}\label{fig:kanban}
\end{figure}

\subsection{Planificació}\label{subsec:planificacio}
La planificació del projecte s'ha dividit en 7 tasques llistades a la taula.\pref{tab:tableplan}

\begin{table}
    \centering
    \caption{Tasques amb la descripció}
    \begin{tabular}{|c|c|}
        \hline
        \begin{tabular}[c]{@{}c@{}}
            Disseny de les \\classes principals
        \end{tabular}                            & \begin{tabular}[c]{@{}c@{}}
                                                       Dissenyar les classes de \\renderització i els altres\\components del joc.
                                                   \end{tabular}               \\
        \hline
        \begin{tabular}[c]{@{}c@{}}
            Disseny de la \\interfície del sudoku
        \end{tabular}                          & \begin{tabular}[c]{@{}c@{}}
                                                     Dissenyar les diferents \\pantalles del joc: la pantalla\\d'inici, els controls del\\sudoku\ldots
                                                 \end{tabular}              \\
        \hline
        \begin{tabular}[c]{@{}c@{}}
            Desenvolupament de \\les classes principals\\del joc
        \end{tabular} & \begin{tabular}[c]{@{}c@{}}
                            Implementar en la classe de \\renderització amb OpenGL,\\implementar les rutines de\\tractament de les accions\\dels perifèrics.
                        \end{tabular}                                        \\
        \hline
        \begin{tabular}[c]{@{}c@{}}
            Desenvolupament del \\joc
        \end{tabular}                                    & \begin{tabular}[c]{@{}c@{}}
                                                               Implementar la lògica i les \\funcionalitats principals del\\joc.
                                                           \end{tabular} \\
        \hline
        \begin{tabular}[c]{@{}c@{}}
            Tests
        \end{tabular}                                    & \begin{tabular}[c]{@{}c@{}}
                                                               Tests per a cada component \\implementat.
                                                           \end{tabular}
        \hrulefill
    \end{tabular}\label{tab:tableplan}
\end{table}

\section{Desenvolupament}\label{sec:development}

\begin{figure*}[!h]
    \centering
    \includegraphics[width=0.9\textwidth]{img/uml}
    \includegraphics[width=0.9\textwidth]{img/uml2}
    \caption{Diagrama UML del projecte.}
\end{figure*}

Per desenvolupar un joc, normalment, s'utilitza un motor de joc que prové les eines necessàries per
al ràpid desenvolupament del videojoc com per exemple \textit{Unreal Engine (UE)} o \textit{Godot}.
La funcionalitat bàsica que normalment proporciona un motor de joc pot incloure entre altres: un
motor de renderització per a gràfics en 2D o 3D, un motor físic per a la detecció de col·lisions i
la simulació de físiques i un sistema de gestió d'entitats. En el nostre cas, com que no hem
implementat un joc molt complex, no ha fet falta desenvolupar un motor de renderització ni un motor
de física; només s'ha implementat certes funcionalitats claus per al desenvolupament del nostre
joc. És a dir un sistema per renderitzar objectes, un sistema d'esdeveniments per gestionar la E/S
de l'usuari i diverses estructures de dades per a la gestió de l'estat.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.5\textwidth]{img/godot}
    \caption{L'entorn de desenvolupament del motor de joc \textit{Godot}.}\label{fig:godot}
\end{figure}

\subsection{Inicialització}\label{subsec:inicialitzacio}
Abans de res, necessitem una finestra i un context OpenGL per a poder renderitzar els objectes a la
pantalla. Hi ha diverses funcions que s'encarreguen de la creació i de la destrucció del context
OpenGL a més de la instal·lació dels \textit{callback} dels esdeveniments per als perifèrics.

\begin{itemize}
    \item \textbf{InitWindow(title, width, height)}: Crea una finestra amb mides \textbf{width $\times$ height} amb el títol \textbf{title} i carrega les funcions d'OpenGL cridant a \textit{glbinding::Binding::initialize().}
    \item \textbf{ConfigureEvents()}: Insta-la els \textit{callback} a la finestra creada i configura la classe \textit{EventState.}
    \item \textbf{DestroyWindow()}: Destrueix la finestra i allibera la memòria assignada per \textit{InitWindow.}
\end{itemize}

\subsection{Sistema de renderització}\label{subsec:sistema-de-renderitzacio}
El sistema de renderització està compost per dues classes:

\begin{itemize}
    \item La classe \textit{Object}, és la classe base per a qualsevol objecte que es dibuixa a pantalla
          (\textit{Cube, Sphere, Plane, Crosshair}). Té dos mètodes principals:
          \begin{itemize}
              \item \textbf{setupVao(position, normals, texture, index)}: Inicialitza les estructura de dades i els \textit{shaders} necessàris per al dibuixat amb \textit{OpenGL}.
              \item \textbf{draw(transform, color, isLightEnabled, drawNormals)}: El mètode amb el qual es dibuixa a la pantalla.
                    El mètode aplica l'escalat, la rotació, la translació i el segons els arguments.
                    Els arguments \textbf{isLightEnabled} i \textbf{drawNormals} permeten activar la il·luminació i dibuixar les normals de l'objecte.
          \end{itemize}

    \item La classe \textit{Shader}, s'encarrega de compilar i generar els programa \textit{shader} a partir
          d'un codi font i assignar les variables \textit{uniforms} del programa. Entre altres mètodes es
          troben:
          \begin{itemize}
              \item \textbf{loadShader(path)}: Compila els \textit{shaders} indicat per l'argument \textit{path} i crea el programa \textit{shader}, també realitza una comprovació d'errors.
              \item \textbf{setLight(light)}: Si el programa \textit{shader} suporta il·luminació, configurarà la il·luminació amb l'argument.
                    Si no donarà error.
              \item \textbf{setMaterial(material)}: Si el programa \textit{shader} suporta materials, configurarà el material amb l'argument i almenys hi ha dues textures.
                    Si no donarà error.
          \end{itemize}
\end{itemize}

\subsubsection{Cub}
La renderització d'un cub es fa partir de triangles, primer es defineix la posició dels vèrtexs
dels triangles que el conformen, a més de les normals i de les coordenades de la textura i
s'utilitza el mètode \textbf{setupVao} de la classe base per enviar les dades dels vèrtex a la
GPU.\@

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/cube}
    \includegraphics[width=0.4\textwidth]{img/sphere}
    \caption{Renderització d'un cub i una esfera amb il·luminació i les seves normals.}\label{fig:cube}
\end{figure}

\subsubsection{Esfera}

De la mateixa manera que el cub, és necessari les posicions dels vèrtexs i les normals, però en
aquest cas per construir una esfera amb triangles primer s'ha de particionar la superfície en
diferents sectors i estacs, ja que no podem dibuixar tots els punts de l'esfera en pantalla.

\subsubsection{Fitxa del sudoku}
Amb un cub i una esfera es pot renderitzar l'entitat principal del joc, és necessari un cub i sis
esferes (encara que només s'utilitza 4 esferes pels números del 0 al 9). El dibuixat de les esferes
es fa amb un mapajat entre els valors numèrics i les posicions de les esferes en la fitxa.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/tile}
    \caption{El numero 7 i 2 en braille.}\label{fig:tile}
\end{figure}

\subsubsection{Pla}

Aquest objecte l'utilitzem per seleccionar el pla que es vol en la funció \textit{glUnproject} per
convertir la posició del ratolí en la finestra a coordenades món. Si no s'utilitza un pla, la
posició del món que s'obtindria seria la del pla \textit{zFar}.

\subsubsection{Mira}
La mira, a diferència dels objectes anteriors, és una figura 2D per això la classe té un mètode per
renderitzar especial que fa l'ús de la projecció ortogràfica i desactiva la il·luminació.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/crosshair}
    \caption{Visualitzats amb RenderDoc dels vèrtexs enviats al \textit{shader}.}\label{fig:crosshair}
\end{figure}

\subsection{Classes d'esdeveniments}\label{subsec:classes-d'esdeveniments}
La classe \textit{EventState} gestiona l'estat dels perifèrics, aquesta classe és necessari perquè
utilitzem la llibreria \textit{ImGUI} i necessitem diferenciar entre els esdeveniments dirigits al
joc dels esdeveniments dirigits als a la llibreria.\\ Aquesta classe té mètodes per obtenir l'estat
de qualsevol botó i tecla, concretament s'han implementat els següents mètodes:

\begin{itemize}
    \item \textbf{keyPressed(key)}: Retorna 1 si la tecla ha sigut pressionada 0 en cas contrari.
    \item \textbf{keyDown(key)}: Retorna 1 si la tecla està pressionada, 0 en cas contrari.
    \item \textbf{keyReleased(key)}: Retorna 1 si la tecla ha deixat d'estar pressionada 0 en cas contrari.
    \item \textbf{mouseButtonPresssed(key)}: Igual que les tecles però amb els botons del ratolí.
    \item \textbf{mouseButtonDown(key)}: Igual que les tecles.
    \item \textbf{mouseButtonReleased(key)}: Igual que les tecles.
    \item \textbf{isMouseMoved()}: Retorna 1 si l'usuari ha mogut el ratolí 0 en cas contrari.
    \item \textbf{getMousePosition()}: Retorna les coordenades (x, y) del ratolí.
    \item \textbf{isMouseScrolled()}: Retorna 1 si l'usuari ha mogut la roda del ratolí 0 en cas contrari.
    \item \textbf{getMouseScroll()}: Retorna la diferència (x0-x1, y0-y1) de l'última posició de la roda.
    \item \textbf{isWindowSizeChanged()}: Retorna 1 si l'usuari si l'usuari ha redimensionat la finestra, 0 en cas contrari.
    \item \textbf{getWindowSize()}: Retorna la mida de la finestra.
    \item \textbf{isWindowMoved()}: Retorna 1 si l'usuari si l'usuari ha mogut la finestra, 0 en cas contrari.
    \item \textbf{getWindowPosition()}: Retorna la posició (x, y) de la finestra.
\end{itemize}

A més dels d'aquests mètodes per obtenir l'estat dels perifèrics i de la finestra, estan els
mètodes que s'utilitzen per actualitzar l'estat.

\begin{itemize}
    \item \textbf{mouseHandler(button, action, mods)}: Actualitza l'estat dels botons del ratolí.
    \item \textbf{keyboardHandler(key, action ,mods)}: Actualitza l'estat de les tecles.
    \item \textbf{mouseMoveHandler(x, y)}: Actualitza la posició del ratolí.
    \item \textbf{mouseScrollHandler(key)}: Actualitza la posició de la roda del ratolí.
    \item \textbf{windowSizeHandler(width, height)}: Actualitza la mida de la finestra.
    \item \textbf{windowPositionHandler(x, y)}: Actualitza la posició de la finestra.
    \item \textbf{onUpdate(frametime)}: Restableix l'estat després de cada \textit{frame} i actualitza l'estat del joc.
\end{itemize}

\subsection{Càmera}\label{subsec:classe-camera}
La classe \textit{Camera} conté les matrius que s'utilitzen per posicionar i transformar els
objectes al món, més concretament, proveeix dues matrius dependentment si es vol una projecció
ortogràfica o una projecció perspectiva. Si l'objecte que es vol dibuixar en pantalla és una figura
3D, com ara l'esfera o el cub, s'utilitza la projecció perspectiva i si no, s'utilitza la projecció
ortogràfica per dibuixar els objectes a pantalla.
\begin{figure}[!h]
    \centering
    \includegraphics[width=0.5\textwidth]{img/transforms}
    \includegraphics[width=0.4\textwidth]{img/pers}
    \caption{Per a una geometria 3D és necessari 3 matrius que transformen les coordenades locals a coordenades món, si fos un element del HUD (com és el cas de la mira) només seria necessari la matriu \textit{uModel} (per transformar els vèrtexs) i la \textit{uProjection}.}\label{fig:pers}
\end{figure}

\subsection{Selecció d'objectes}\label{subsec:picking-object}
Una de les funcions més importants de qualsevol joc és la selecció d'objectes, una manera
d'implementar la selecció és utilitzant una tècnica anomenada \textit{Picking Object}. La idea
principal és codificar l'identificador de l'objecte en un color únic i pintar els objectes amb
aquest color, després, es recupera el color que està sota el ratolí i obtenim l'identificador a
partir del color. Una de l'avantatge d'aquest mètode és la seva simplicitat comparada amb altres
tècniques que utilitzen un motor físic per projectar una recta en la posició ratolí i buscar
col·lisions amb els objectes. Però l'inconvenient que té aquesta tècnica és que disminueix molt els
fotogrames per segon pel fet que s'ha de dibuixar l'escena dues vegades (una pel \textit{picking
    object} i una per l'escena normal).

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/drawPick}
    \caption{Visualització dels colors assignats als objectes.}\label{fig:pick}
\end{figure}

\subsubsection{Implementacó}\label{subsec:implementacio}

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/genColor}
    \caption{Funció de generació del color.}\label{fig:genColor}
\end{figure}

Com s'ha explicat en l'apartat anterior, per utilitzar la tècnica del \textit{picking object} es
necessari un transformar un identificar a un color únic. Per desenvolupament del projecte s'ha
optat per utilitzar una transformació que associa cada identificador amb un color RGB amb
l'algoritme de la figura \pref{fig:genColor}.

La funció \textbf{colorToId(color)} \pref{fig:getColor} obté l'identificador a partir del color.
\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/getColor}
    \caption{Funció de obtenció del identificador.}\label{fig:getColor}
\end{figure}

A més a més de les transformacions, s'utilitza la funció \textbf{glReadPixels(x, y, type, dataType,
    out)} per obtenir el color en la posició del ratolí. Amb aquesta funció es pot obtenir, a més del
color, la posició de la coordenada Z que s'utilitza per saber a quin pla s'ha de col·locar
l'objecte seleccionat.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/glRead}
    \caption{Utilització de la funció glReadPixels.}\label{fig:glRead}
\end{figure}

\subsection{Sudoku}\label{subsec:sudoku}
Aquesta classe té les següents funcions: inicialitzar el joc, comprovar que l'estat del joc és
vàlid, generar i carregar sudokus. La inicialització col·loca les fitxes en la posició correcta i
inicialitza l'estructura de dades \textit{Tile} que conté Informació relativa a la posició i el
valor de la fitxa. Per comprovar que el joc s'ha completat correctament, sumem totes les cel·les i
es comprova que igual a 405 ($(1+2+...+9) * 9$). La classe permet generar sudokus amb tres nivells
de dificultat (3, 5 i 7 caselles buides); per generar un sudoku, primer, emplenem les subgraelles
de la diagonal que són independents entre si, després, utilitzem un algoritme de backtracking per
arribar a una solució i trèiem valors d'una fila i columna aleatòria fins a arribar a la dificultat
desitjada.

\subsection{Classe Món}\label{subsec:world}
És la classe que conté el bucle del joc i les accions permeses del jugador. La classe conté referències a la fitxa seleccionada, la fitxa que sota el ratolí i la fitxa més pròxima al ratolí (selectedTile, hoveredTile i nearesTile). Amb aquestes tres variables podem implementar totes les accions requerides pel jugador.
\\
La variable nearesTile conté la fitxa que està mes a prop del ratolí, s'actualitza per cada frame utilitzant la funció \textbf{nearestTile(pos, predicate)}, el argument \textit{predicate} serveix per ometre fitxes que compleixin una certa condició (com ara ometre la fitxa seleccionada, ja que és sempre la més propera).
\\
La variable hoveredTile s'obté mitjançant la funció \textbf{colorToId(color)} amb el color de la posició del ratolí i representa la fitxa que se seleccionarà si es fa clic.
\\
Si es fa clic, la variable selectedTile s'assigna amb el valor de hoveredTile, podem moure la fitxa pel pla del si es manté el botó del ratolí polsat. Amb la fitxa seleccionada deixar-la anar en la fitxa indicada per la variable nearesTile que es permetrà assignar números (si és correcte)  als llocs buits.
\\
Les fitxes permeten donar pistes en fer un clic (mostra la solució parcial en braille fins a un màxim de 3 esferes) i reprodueix el número en anglès. L'àudio és reprodueix fent servir un thread a part del main, només s'ha implementat en Linux utilitzant l'API de baix nivell de Pipewire. La funció \textbf{LoadAudioFile(path)} pot carregar fitxers WAV amb dos canals a 44,1 kHz a causa de la implementació de la reproducció d'àudio.
\\
\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/tauler}
    \caption{Estat inicial de joc.}\label{fig:tauler}
\end{figure}

La interfície està implemetada amb ImGUI, una llibreria que permet crear GUI de forma declarativa
de la mateixa manera que el framework Flutter o Android Compose. La llibreria té varis widgets com
ara: checkboxes, sliders per a scalars i vectors, modal popups \ldots que permeten la creació
d'interfície fàcilment.

El menú principal conté botons per a carregar sudokus i generar sudokus de diferents dificultats i
diversos checkboxes per a activar i desactivar opcions i finestres de control (control de la
càmera, de la il·luminació \ldots) del joc.

\begin{figure}[!h]
    \centering
    \includegraphics[width=0.4\textwidth]{img/menu}
    \includegraphics[width=0.4\textwidth]{img/overlay}
    \caption{Menú i overlay del joc.}\label{fig:menu}
\end{figure}

\section{Resultats}\label{sec:results}

Com a resultats d'aquest projecte, s'ha completat exitosament el desenvolupament del sudoku 3D i
dels objectius proposats inicialment. El joc pot ser compilat utilitzant CMake en les dues
plataformes proposades i el joc arriba als 60 fps mínims per a jugar.

\section{Conclusions}\label{sec:conclusion}
Aquest projecte ha sigut una primera experiència al desenvolupament de jocs amb OpenGL amb la
utilització de shaders, s'ha pogut expandir el coneixement sobre els gràfics 3D i al
desenvolupament de programes en diferents plataformes.

\section*{Agraïments}

\begin{thebibliography}{11}
    \bibitem{latex}
    http://en.wikibooks.org/wiki/LaTeX

    \bibitem{2}
    Referència 2

    \bibitem{3}
    Etc.

\end{thebibliography}

% \appendix
% \setcounter{section}{1}
% \section*{Apèndix}

\end{document}